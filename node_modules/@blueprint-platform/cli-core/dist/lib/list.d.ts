/** Layout shell names available via `foundation:layout` (excludes `auth-split`/`auth-centered` — see B1). */
export declare function listLayouts(registry?: string, cwd?: string): Promise<string[]>;
/** Every UI-kit component name available via `components:ui` (see B2). */
export declare function listComponents(registry?: string, cwd?: string): Promise<string[]>;
export declare function listModules(registry?: string): Promise<Array<{
    name: string;
    description: string;
}>>;
//# sourceMappingURL=list.d.ts.map